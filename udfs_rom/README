I took Martin Mathers' MMBEEB patched Acorn DFS v0.9 ROM and changed it so
instead of reading disk image data from an MMC card, it would request bytes
over USB from the disk image loaded into the "server" program on the host side.
